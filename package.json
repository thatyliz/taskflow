{
  "name": "taskflow",
  "version": "1.0.0",
  "description": "Sistema CRUD de Tarefas com Observabilidade — pronto para OpenTelemetry + Grafana Stack",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "migrate": "node src/config/migrate.js",
    "simulate:failures": "node scripts/simulate-failures.js",
    "lint": "eslint src/**/*.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "winston": "^3.11.0",
    "prom-client": "^15.1.0",
    "uuid": "^9.0.0",
    "dotenv": "^16.3.1",
    "express-async-errors": "^3.1.1",
    "compression": "^1.7.4",
    "helmet": "^7.1.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
